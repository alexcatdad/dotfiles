name: Validate Fresh Installation

# NOTE: This workflow was disabled because Docker tests need migration to new Bun-based CLI
# The old bash-based install scripts (bootstrap.sh, install-safe.sh) are deprecated
# in favor of the new Bun CLI (./dist/dotfiles)

on:
  schedule:
    # Test fresh installation weekly
    - cron: '0 6 * * 1'

jobs:
  placeholder:
    name: Docker tests disabled - pending Bun CLI migration
    runs-on: ubuntu-latest
    if: false
    steps:
      - run: echo "Docker tests removed - old bash scripts no longer in use"